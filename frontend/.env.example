# Base URL used by the SPA for every API request. Keep "/api" so the Vite dev
# proxy (and the production reverse proxy) can forward calls to Spring Boot.
VITE_API_BASE_URL=/api

# Local development only: where the Vite dev server proxies API requests to.
VITE_DEV_API_TARGET=http://127.0.0.1:8080
